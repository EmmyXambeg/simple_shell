#include "main.h"

/**
 * _strlen - funct that Returns length of the string.
 * @s: Type char pointers.
 * Return: null.
 */
int _strlen(const char *s)
{
	int len;

	for (len = 0; s[len] != 0; len++)
	{
	}
	return (len);
}
